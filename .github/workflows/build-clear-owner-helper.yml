name: Build clear-owner helper

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build temporarily paused while converting helper into same-package App Manager update."
